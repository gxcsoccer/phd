# Create an application at https://www.producthunt.com/v2/oauth/applications
# then copy the credentials here. For GitHub Actions, set the same keys as
# repository secrets (PH_CLIENT_ID, PH_CLIENT_SECRET) instead of using .env.
PH_CLIENT_ID=
PH_CLIENT_SECRET=
